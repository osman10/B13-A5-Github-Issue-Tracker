<h1>1️⃣ What is the difference between var, let, and const?</h1> <br>
<p>var is global scope can be reassign and redeclear, Block-scoped Cannot be redeclared Can be reassigned and const Block-scoped Cannot be redeclared Cannot be reassigned. </p>
<h1>2️⃣ What is the spread operator (...)?</h1>
<p>It is used for spread array objects instead of writing the objects </p>
<h1>3️⃣ What is the difference between map(), filter(), and forEach()?</h1>
<p>map() makes a new array forEach() makes a function for the each elements and filter() return new array based on a condition </p>
<h1>4️⃣ What is an arrow function?</h1>
<p>Is is ES6 features can be written shortly using ()=>{} symbol and can declear in one line like this: ()=>. </p>
<h1>5️⃣ What are template literals?</h1>
<p>We use (``) this symbol instead of ("") this symbol so that we can write multiline, can write html tag and also write function inside it where in the ("") symbol we can write only string.</p>
